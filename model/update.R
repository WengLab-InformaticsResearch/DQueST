updateWMatrix = function(wMatrix, common_concept_id, answer){
  # update wMatrix.
  wMatrix_new = NULL
  tryCatch(expr = {1},
           error = function(e) e,
           finally = print("update wMatrix"))
  return(wMatrix_new)
}

